apiVersion: v1
kind: Service
metadata:
  name: __SERVICE_NAME__
spec:
  selector:
    app: __APP_NAME__
  type: ClusterIP
  ports:
    - name: tds
      protocol: TCP
      port: 1433
      targetPort: 1433
  # Internal-only ClusterIP service for in-cluster access (no external exposure)
